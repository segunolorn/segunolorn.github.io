export const informalGreeting = (person) => {
    console.log(`Hello, ${person}`);
}

export const formalGreetimg = (person) => {
    console.log(`Good day, ${person}`);
}

const greeting = () => {
    console.log(`Howdy!`);
}

export default greeting;